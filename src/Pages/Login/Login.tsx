import { LoginContent } from "../../Components";
import { SigningUpLayout } from "../../Layout";

export const Login = () => {
	return (
		<SigningUpLayout>
			<LoginContent />
		</SigningUpLayout>
	);
};
