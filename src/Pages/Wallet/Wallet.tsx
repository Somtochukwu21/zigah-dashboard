import React from "react";
import { Layout } from "../../Layout";
import { Card } from "../../Global";

export const Wallet = () => {
	return (
		<Layout>
			<Card>
				<div>Wallet</div>
			</Card>
		</Layout>
	);
};
